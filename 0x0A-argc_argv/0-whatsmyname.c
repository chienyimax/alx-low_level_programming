#include <stdio.h>
#include <stdlib.h>

/**
 * main - a program that receives arguments
 * @argc: int
 * @argv: char pionting to a strings
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{

	printf("%s\n", argv[0]);

	return (0);
}
